Those left/right shifter uses temary, which could be replaced by mux.
barrelShifter has two different implementations (the other is commented).
barrelShifter could use mux to implement shift by each bit.